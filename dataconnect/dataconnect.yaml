specVersion: "v1beta"
serviceId: "myapp"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "myapp-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
